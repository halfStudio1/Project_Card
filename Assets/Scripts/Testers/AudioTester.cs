using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AudioTester : MonoBehaviour
{
    private AudioSource audioSource;
    // Update is called once per frame
    void Update()
    {
        //if (Input.GetKeyDown(KeyCode.A))
        //{
        //    ResMgr.Instance.LoadAssetAsync<AudioClip>("Royal Days", (clip) =>
        //    {
        //        //AudioMgr.Instance.PlayMusic(clip,4f);

        //        audioSource = AudioMgr.Instance.PlaySE(clip);

        //        audioSource.PlayOneShot(clip);

        //        AudioMgr.Instance.PlayAmbience(clip);
        //    });
        //}

        //if (Input.GetKeyDown(KeyCode.S))
        //{
        //    //AudioMgr.Instance.StopMusic(4f);

        //    AudioMgr.Instance.StopSE(audioSource);

        //    AudioMgr.Instance.StopAmbience();
        //}
    }
}
